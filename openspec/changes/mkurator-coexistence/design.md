# Coexistence design

**Status:** Deferred until ADR-0007 is accepted.

Must define the discovery configuration, ownership cache freshness rules, the
degradation matrix (no cluster access, no permission, stale data), and the
hook contract used by guarded administration.
